<nav class="flex md:flex-col flex-wrap gap-4">
    {
        [
            ["home", "/"],
            ["about", "/about"],
            ["projects", "/projects"],
            ["photos", "/photos"],
            ["thoughts", "/thoughts"],
        ].map(([name, path]) => (
            <a href={path} class="text-2xl hover:translate-x-2 transition">
                {name}
            </a>
        ))
    }
</nav>

